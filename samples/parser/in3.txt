int c,e,b; 
int a:(c - b * -e + -b); 
bool g:( a > b and e != c );
void someMethod2 (int a, b1, b2) {
    if ( a + b2 > b1)
        if ( a - b2 == b1)
            a = b++;
        other
            b + c;
}